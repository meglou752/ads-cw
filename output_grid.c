#include "sudoku.h"

/*
 * Logic to remove numbers from the output grid and check if it is still unique/valid for the specified difficulty
 * Switch statement for difficulty
 * case 1; remove 25, case 2; remove 30, case 3; remove 35
 * check uniqueness: call backtracking on this solution forwards and backwards
 * if either solution != initial solution, remove numbers again until either solution == initial solution
 */
// Needs implementation
//will need board


// Needs implementation
int check_uniqueness()
{
    return 0;
}

void generate_unique_solution(int num_to_remove)
{

    //display_board();
    //remove_numbers(num_to_remove);
    /*
    while(!check_uniqueness())
    {
        remove_numbers(num_to_remove);
    }
     */
}
// Needs implementation
void handle_input()
{

}